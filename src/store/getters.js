export const code = (state) => state.code;

export const prefix = (state) => state.prefix;

export const queryCode = (state) => state.queryCode;

export const historyList = (state) => state.historyList;
